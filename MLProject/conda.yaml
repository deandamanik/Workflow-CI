name: tokopedia_model_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - pandas
      - scikit-learn
      - mlflow
      - dagshub
      - matplotlib
      - seaborn